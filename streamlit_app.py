import streamlit as st

# --- Конфігурація сторінки ---
st.set_page_config(
    page_title="Простий Веб-інтерфейс Streamlit",
    page_icon="💡",
    layout="centered"
)

# --- Заголовок та опис ---
st.title("💡 Мій перший Веб-інтерфейс на Streamlit")
st.markdown("""
Цей простий застосунок демонструє базові можливості Streamlit:
введення тексту та відображення результатів.
""")

st.subheader("Введення даних")

# --- Елемент для введення тексту ---
user_input = st.text_input(
    label="Введіть ваше ім'я або повідомлення:",
    placeholder="Наприклад: Привіт, Світ!"
)

# --- Кнопка для обробки ---
if st.button("Надіслати"):
    if user_input:
        # Відображення результату, якщо текст введено
        st.success(f"👋 Привіт, **{user_input}**! Дякую, що скористалися застосунком.")
        
        # Додатковий елемент
        st.balloons()  # Додає ефект кульок
    else:
        # Попередження, якщо поле пусте
        st.warning("Будь ласка, введіть щось у поле для тексту.")

# --- Додатковий елемент (Сайдбар) ---
with st.sidebar:
    st.header("Налаштування")
    st.write("Це бічна панель, яку можна використовувати для додаткових опцій.")
    slider_value = st.slider("Оберіть значення:", 0, 100, 50)
    st.info(f"Ви обрали значення: {slider_value}")

# --- Нижній колонтитул ---
st.markdown("---")
st.caption("Розроблено за допомогою Streamlit.")
